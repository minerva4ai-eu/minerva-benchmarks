# Base configuration for benchmark experiments
defaults:
  - MN5
  - _self_

machine:
  runtime_env_mode: singularity
  framework_name_pattern: MN5-cuda128
  modules:
  - singularity
  singularity_args:
    - "--nv"
    - "--network=host"