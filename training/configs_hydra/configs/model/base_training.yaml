training:
  batch_size: 1
  grad_accum: 1
  precision: "bf16"
  steps: 50
  lr: 2e-5
  optimizer: "adamw"
  gradient_checkpointing: False
  enable_compile: True
  epochs: null

combinations:
  batch_sizes: [1, 4, 8]
  precisions: ["bf16", "fp32"]
  grad_accums: [8, 16]
  lr: [2e-5]
  optimizer: ["adamw"]
  gradient_checkpointing: [False] # TODO: Implement parameterization
  steps: [50]
  enable_compile: [False, True]
