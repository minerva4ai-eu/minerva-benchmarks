account: bsc99
qos: acc_bench
partition: acc


sbatch:
  gpus_per_node: 4
  cpus_per_gpu: 20
  chdir: ??
  nodes: 1
  gres: gpu:${arch.node.gpus_per_node}
  cpus_per_task: 80
  tasks_per_node: 1
  output: run-%j.out
  error: run-%j.err
  extra_args:
    - "--exclusive"
    - "--time=00:30:00"

srun:
  gpus_per_node: 4
  cpus_per_gpu: 20
  nodes: 1
  cpus_per_task: 80
  tasks_per_node: 1