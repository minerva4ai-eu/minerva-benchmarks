[project]
name = "slurm-cli-minimal-env"
version = "0.1.0"
requires-python = "==3.11.*"
dependencies = [
  "setuptools",
  "matplotlib",
  "numpy",
  "pandas",
  "seaborn",
  "pyyaml",
  "python-dotenv",
  "tqdm",
  "psutil",
  "prompt-toolkit",
  "hydra-core>=1.3.2",
  "click>=8.4.1",
]
