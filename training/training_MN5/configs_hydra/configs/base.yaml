# Base configuration for benchmark experiments
defaults:
  - trainings: combinations
  - model: base_training
  - framework: base
  - dataset: base
  - _self_

machine:
  name: ??? # self given name, must coincide with .env-{machine.name} file
  name_pattern: ???
  modules: ??? # space separated modules to load
  python_environment: ??? # path to python virtual environment
  singularity_container: ???

experiment:
  name: "benchmark_run"
  output_dir: "results/"
